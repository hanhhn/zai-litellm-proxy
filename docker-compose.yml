version: '3.8'

services:
  litellm:
    image: ghcr.io/berriai/litellm:latest
    container_name: zai-litellm-proxy
    ports:
      - "4000:4000"
    volumes:
      - ./config.yaml:/app/config.yaml
    environment:
      - ZAI_API_KEY=${ZAI_API_KEY}
    command: litellm --port 4000 --drop_params
    restart: unless-stopped
